/** Injected on row objects during start-process so matches can be persisted with stable DB ids. */
const INTERNAL_SALES_ROW_ID = "__naviSalesRowId";
const INTERNAL_PDF_ROW_ID = "__naviPdfRowId";

module.exports = {
  INTERNAL_SALES_ROW_ID,
  INTERNAL_PDF_ROW_ID,
};
