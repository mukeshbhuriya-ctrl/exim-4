const {
  getStoredSapCredentials,
  upsertSapCredentials,
  sanitizeSapCred,
} = require("#utils/configure");

module.exports = {
  getStoredSapCredentials,
  upsertSapCredentials,
  sanitizeSapCred,
};
